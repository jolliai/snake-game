{
	"schemaVersion": 1,
	"stableSlug": "rainbow-snake-surprise",
	"title": "Rainbow Snake Surprise (JOE-7)",
	"content": "## Overview\n\nJOE-7 adds a rare easter egg: on roughly 1 in 100 games, player 1's snake renders as an animated rainbow and a celebratory banner appears. The effect must not alter normal gameplay, must persist for the entire game session once activated, and must be visibly animated (not gated behind `prefers-reduced-motion`).\n\n## Files Changed\n\n- `src/main.ts` — roll logic, state field, URL seam, `draw()` per-segment coloring, banner show/hide methods\n- `index.html` — `#rainbow-banner` element\n- `src/style.css` — `.rainbow-banner` styles and keyframe animations\n\n## Per-Game Roll\n\nA constant `RAINBOW_CHANCE = 0.01` is defined near the other tuning constants. The `SnakeGame` class holds a private `rainbowSnake: boolean` field. In `startNewGame()`, after the state reset block, the field is set:\n\n```ts\nthis.rainbowSnake = this.rainbowOverride ?? (Math.random() < RAINBOW_CHANCE);\n```\n\n### URL Override Seam\n\nBecause a 1-in-100 event cannot be reliably exercised in automated tests, a `?rainbow=1` or `?rainbow=0` query parameter overrides the roll deterministically. The override is parsed once in the constructor into `private rainbowOverride: boolean | null`. This has no effect in production.\n\n## Rainbow Rendering\n\nPer-segment rainbow coloring is implemented in the `draw()` method. Each player-1 snake segment is assigned an HSL hue derived from its position along the body:\n\n```\nh = ((index / len) * 360 + phase) % 360\n```\n\nThe `phase` value scrolls over time using `performance.now()`, producing an animated shimmer. The existing per-frame canvas redraw (driven by the board auto-rotation rAF loop) animates the effect for free — no additional animation loop is needed.\n\nA helper `rainbowFaces(index, len, phase)` returns three HSL face shades matching the existing isometric shading model (top lightest → left darkest):\n- Top: `hsl(h, 90%, 62%)`\n- Left: `hsl(h, 90%, 50%)`\n- Right: `hsl(h, 90%, 40%)`\n\nHSL strings are valid `ctx.fillStyle` values directly, so no hex conversion is needed (unlike the existing `lerpHex` helper, which is hex-only).\n\n**Scope:** Player 1's snake only. In two-player modes, player 2's snake remains blue so player identity is preserved.\n\n**Hunger tint interaction:** Rainbow coloring supersedes the green→warning `tint()` blend for body colors during a rainbow game. The starvation cue is not lost — it is still carried by the pulsing starvation text banner (`updateStarvationWarning`).\n\n## \"FRIENDSHIP!\"-Style Banner\n\nThe banner is styled after the classic Mortal Kombat friendship finisher. Text reads **\"Rainbow Snake\"** in chunky all-caps arcade lettering.\n\n### HTML (`index.html`)\n\nInside `.canvas-wrap`, alongside `#death-banner`:\n\n```html\n<div id=\"rainbow-banner\" class=\"rainbow-banner hidden\" role=\"status\">\n  <div class=\"rainbow-arc\" aria-hidden=\"true\"></div>\n  <div class=\"rainbow-text\">Rainbow Snake</div>\n</div>\n```\n\n### CSS (`src/style.css`)\n\n- `.rainbow-banner` — absolute-positioned, centered near the top of the board, `pointer-events: none`. Reveals with a **bounce pop-in** (`friendship-pop` keyframe: scale from 0.5→1 with a cubic-bezier overshoot). Dismisses with a CSS opacity fade.\n- `.rainbow-arc` — a semicircular rainbow rendered via a bottom-anchored `radial-gradient` clipped to the top half, with concentric bands of red→orange→yellow→green→blue→indigo→violet sitting behind the text.\n- `.rainbow-text` — `text-transform: uppercase`, `font-weight: 900`, generous `letter-spacing`, bright yellow (`#ffe600`) fill with a thick dark outline built from layered `text-shadow`s. Flickers three times via a `friendship-flash` blink animation (`steps(2), 3 iterations`) before settling.\n\n### Logic (`src/main.ts`)\n\nModelled on the existing `showDeathBanner`/`hideDeathBanner` pattern:\n\n- `showRainbowBanner()` — removes `hidden`, then schedules `hideRainbowBanner()` via `window.setTimeout` after 3000 ms (timer ID stored in `rainbowBannerTimer`).\n- `hideRainbowBanner()` — adds `hidden` and calls `clearTimeout` on the stored timer.\n- Called from `startNewGame()` at the end, once the game screen is shown.\n- Also called in reset, game-over, and back-to-menu paths via `showScreen` so a stale banner never lingers.\n\n## Testing\n\nA `?rainbow=1/0` URL parameter forces the outcome for Playwright tests and manual verification. The recommended E2E assertions are:\n- `?rainbow=1`: `#rainbow-banner` is visible with text \"Rainbow Snake\", then auto-hides after ~3 s.\n- `?rainbow=0`: banner remains hidden throughout.\n- Two-player mode with `?rainbow=1`: player 2's snake stays blue.\n\nCanvas pixel colors are not asserted — the DOM banner and forced flag are the reliable observables.",
	"relatedBranches": [
		"josephhart/joe-7-add-rainbow-colored-snake-suprise"
	],
	"sourceRefs": [
		{
			"type": "summary",
			"id": "a186ad204e48dc5770e2761f453e391ab34d240a",
			"timestamp": "2026-07-07T16:00:16.000Z",
			"branch": "josephhart/joe-7-add-rainbow-colored-snake-suprise"
		},
		{
			"type": "plan",
			"id": "optimized-petting-donut-a186ad20",
			"timestamp": "2026-07-07T16:00:43.262Z",
			"branch": "josephhart/joe-7-add-rainbow-colored-snake-suprise"
		}
	],
	"lastUpdatedAt": "2026-07-07T16:01:09.463Z"
}